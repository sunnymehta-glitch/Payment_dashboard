# Server Configuration
PORT=3001

# Twilio WhatsApp Configuration (optional)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_FROM_NUMBER=+14155238886
TWILIO_TO_NUMBER=+1234567890

# Currency / Locale
# Defaults to INR (Indian Rupee) if not set
DEFAULT_CURRENCY=INR






# Google Sheets Configuration (optional)
SHEET_ID=your_google_sheet_id
GOOGLE_CREDS_PATH=./credentials/google-credentials.json
# For serverless (Vercel), you can use JSON content instead of a file path:
# Either of these variable names is accepted (same value):
# GOOGLE_CREDS_JSON= {...full service account JSON...}
# GOOGLE_CREDENTIALS_JSON= {...full service account JSON...}
# Example (escape newlines if single line):
# GOOGLE_CREDS_JSON={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n","client_email":"...","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"https://www.googleapis.com/robot/v1/metadata/x509/..."}

# Zoho Books (multi-org): No special env needed.
# Create webhooks in each Zoho organization pointing to your /webhook URL.
# Optional (if you add signature verification later):
# ZOHO_WEBHOOK_SECRET=your_webhook_secret

# Optional: Default organization fallback (used if webhook doesn't include org data)
# DEFAULT_ORGANIZATION_ID=279657000000000000
# DEFAULT_ORGANIZATION_NAME=Your Company Pvt Ltd
# (Also accepted: ORGANIZATION_ID/ORGANIZATION_NAME or ORG_ID/ORG_NAME)

# Optional: Organization mapping for multi-org setups
# Provide either JSON or simple mapping; used to fill name from ID (or ID from name)
# ORG_MAP_JSON={"279657000000000000":"Financial Vans Main","279657000000000111":"Financial Waste Main"}
# ORG_MAP=279657000000000000=Financial Vans Main;279657000000000111=Financial Waste Main

# Sheet Monitoring (for manual entries)
ENABLE_SHEET_MONITORING=true
SHEET_MONITOR_INTERVAL=30

# Note: If Twilio or Google Sheets credentials are not provided,
# the system will use mock/fallback modes (WhatsApp mock, CSV logging)
# 
# Sheet Monitoring: When enabled, system checks Google Sheets every 30 seconds
# for new manual entries and sends WhatsApp notifications automatically.

