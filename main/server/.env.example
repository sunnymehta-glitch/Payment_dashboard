# Server environment variables (example)
# Copy to `server/.env` and set real values.

# The PORT the server listens on (local development)
PORT=3001

# Optional: explicit public base URL for the server (useful when the server is behind a proxy or deployed)
# If not set, the server will compute base URL from incoming request host in the /health response.
# Example: SERVER_BASE_URL=https://your-deployment-domain.vercel.app
SERVER_BASE_URL=https://payment-broadcast.vercel.app/

# Enable/disable sheet monitoring (set to false on serverless platforms like Vercel)
ENABLE_SHEET_MONITORING=false
